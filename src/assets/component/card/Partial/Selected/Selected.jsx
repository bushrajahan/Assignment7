import React from 'react';
import PropTypes from 'prop-types';

const Selected = ({item}) => {
  return (
    <div>
      <p>{}</p>
    </div>
  );
};

Selected.propTypes = {
  
};

export default Selected;